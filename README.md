# Minimalist Profile Website

A simple single-page profile site built with Vite and React.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Deploy

This project can be deployed to Netlify or Vercel as a static site.
